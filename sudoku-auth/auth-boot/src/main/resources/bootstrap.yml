spring:
  profiles:
    active: dev
  application:
    name: sudoku-auth

common:
  web:
    data-binding: false
    response: false
    api:
      title: 'OAuth2认证中心'
      description: 'OAuth2认证、注销、获取验签公钥接口'
  core:
    projectPackage: 'com.zumin.sudoku.auth'
